version: 4.0.79
steps:
  - prompt: Assert that the homepage loads successfully.
    commands:
      - command: hover-text
        text: CSDN
        description: CSDN logo in the header
        action: hover
      - command: assert
        expect: the CSDN homepage is loaded
  - prompt: Assert that the homepage loads successfully.
    commands:
      - command: hover-text
        text: 首页
        description: Home link in the navigation bar
        action: hover
      - command: assert
        expect: the homepage navigation bar is visible
  - prompt: Assert that the homepage loads successfully.
    commands:
      - command: hover-text
        text: 首页
        description: Home link in the navigation bar
        action: hover
      - command: assert
        expect: the homepage navigation bar is visible
